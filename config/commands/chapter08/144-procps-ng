#!/bin/bash

./configure --prefix=/usr                           \
            --docdir=/usr/share/doc/procps-ng-4.0.4 \
            --disable-static                        \
            --disable-kill

make

su tester -c "PATH=$PATH make check"

make install

